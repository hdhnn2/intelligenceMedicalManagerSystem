<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hd.imms.mapper.AuthUser">
    <select id="getUserList" resultType="com.hd.imms.entity.authorize.User">
        select wid, user_id , user_name,sex, telephone, input_code, hospital_id, bz,password from auth_user
    </select>
    <select id="getUserById" resultType="com.hd.imms.entity.authorize.User">
        select wid, user_id , user_name,sex, telephone, input_code, hospital_id, bz,password from auth_user where user_id=#{userId}
    </select>
    <select id="queryUserRoleById" resultType="com.hd.imms.entity.authorize.UserRole">
        select wid, user_id , role_id from AUTH_USER_ROLE where user_id=#{userId}
    </select>
</mapper>
